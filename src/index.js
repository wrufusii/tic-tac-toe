import "/src/styles.css";

const player = (sign) => {
  this.sign = sign;
};

const gameBoard = (() => {
  const board = new Array(9);
})();

const displayContoller = (() => {
  //todo
  const fields = 1;
})();

const gameContoller = (() => {
  //todo
})();
